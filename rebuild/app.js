document.querySelectorAll('.coverage article').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${y*-2}deg) rotateY(${x*2}deg) translateY(-2px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});const path=document.querySelectorAll('.path line');let p=0;setInterval(()=>{path.forEach((x,i)=>{x.style.opacity=i<=p?1:.18});p=(p+1)%path.length},700);